---
title: 安全与数据边界
description: 明确哪些数据在本地、哪些会被共享，以及如何安全运行。
---

## 边界模型

<FeatureGrid>
  <FeatureCard icon="shield" title="本地工件" description="除非你主动导出或同步，.amem、.avis、.acb 工件默认在本地/运行时环境中生成与管理。" />
  <FeatureCard icon="network" title="客户端配置" description="MCP 配置文件位于本地桌面/运行时配置路径，应按运维凭据处理。" />
  <FeatureCard icon="book" title="运维日志" description="健康账本与诊断日志可能暴露工作流元数据，应按需保护与轮转。" />
  <FeatureCard icon="wrench" title="受控共享" description="只共享必要工件；发布日志或基准前先清理敏感上下文。" />
</FeatureGrid>

## 最小加固清单

- 将备份目录存放在加密存储
- 配置文件使用最小权限
- 轮换 MCP 相关 token/凭据
- 个人与生产工件分离
- 未审查前不要公开原始日志

## 推荐操作实践

<Command
  title="权限加固"
  description="私有备份目录与配置文件的基础权限示例。"
  code={`chmod 700 "$HOME/.agentra-backups"
chmod 700 "$HOME/.claude"
chmod 600 "$HOME/.claude/mcp.json" 2>/dev/null || true`}
/>

<Callout type="warning" title="公开分享注意">
截图和 CLI 日志可能泄露路径、主机名与运维上下文。公开前请先审查。
</Callout>
