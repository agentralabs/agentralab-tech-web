---
title: "Installation"
description: "How to install AgenticContract"
---

## Quick Install (Recommended)

```bash
curl -fsSL https://agentralabs.tech/install/contract | bash
```

This downloads the `agentic-contract-mcp` binary and configures detected MCP clients.

## Package Managers

### npm

```bash
npm install @agenticamem/contract
```

### pip

```bash
pip install agentic-contract
```

### Cargo

```bash
cargo install agentic-contract-cli
```

## From Source

```bash
git clone https://github.com/agentralabs/agentic-contract.git
cd agentic-contract
cargo build --release
```

## Profiles

- **desktop** — Configures Claude Desktop, Cursor, Windsurf
- **terminal** — Configures Claude Code CLI
- **server** — Configures for remote deployment with auth
