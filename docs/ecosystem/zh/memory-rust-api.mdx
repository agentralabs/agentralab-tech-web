---
title: "Rust API 参考"
description: "AgenticMemory is implemented in Rust. This document covers the core library API and the amem CLI tool."
---

AgenticMemory 是用 Rust 实现的。本文档涵盖核心库 API 和 `amem` CLI 工具。

## 图书馆箱

添加到您的 `Cargo.toml`：

```toml
[dependencies]
agentic-memory = "0.2"
```

---

## 认知图

代理认知图的内存表示。这是在将图形写入磁盘之前构建和操作图形的主要数据结构。

### 构造函数

```rust
impl CognitiveGraph {
    /// Creates a new, empty cognitive graph.
    pub fn new() -> Self;
}
```

**例子：**

```rust
use agentic_memory::CognitiveGraph;

let mut graph = CognitiveGraph::new();
```

---

### 添加节点()

```rust
pub fn add_node(
    &mut self,
    event_type: EventType,
    content: &str,
    session: u32,
    confidence: f32,
    metadata: Option<HashMap<String, String>>,
) -> NodeId;
```

将认知事件添加到图表中。返回新节点的唯一 `NodeId`。

**参数：**

|参数|类型 |描述 |
|-----------|------|-------------|
| `event_type` | `EventType` |以下之一：`Fact`、`Decision`、`Inference`、`Correction`、`Skill`、`Episode`。 |
| `content` | `&str` |事件的文字内容。 |
| `session` | `u32` |此事件所属的会话 ID。 |
| `confidence` | `f32` |置信度得分介于 0.0 和 1.0 之间。 |
| `metadata` | `Option<HashMap<String, String>>` | 任选键值元数据。 |

**例子：**

```rust
use agentic_memory::{CognitiveGraph, EventType};

let mut graph = CognitiveGraph::new();
let fact_id = graph.add_node(
    EventType::Fact,
    "Rust 1.75 introduces async fn in traits",
    1,      // session
    0.95,   // confidence
    None,
);
```

---

### 添加边缘()

```rust
pub fn add_edge(
    &mut self,
    source: NodeId,
    target: NodeId,
    edge_type: EdgeType,
    weight: f32,
) -> Result<EdgeId, GraphError>;
```

在两个节点之间创建有向加权边。如果任一节点不存在，则返回 `EdgeId` 或错误。

**参数：**

|参数|类型 |描述 |
|-----------|------|-------------|
| `source` | `NodeId` |源节点ID。 |
| `target` | `NodeId` |目标节点ID。 |
| `edge_type` | `EdgeType` |以下之一：`CausedBy`、`Supports`、`Contradicts`、`Supersedes`、`RelatedTo`、`PartOf`、`TemporalNext`。 |
| `weight` | `f32` |边权重介于 0.0 和 1.0 之间。 |

**例子：**

```rust
use agentic_memory::EdgeType;

let decision_id = graph.add_node(EventType::Decision, "Adopt async traits", 1, 0.9, None);
graph.add_edge(decision_id, fact_id, EdgeType::Supports, 0.85)?;
```

---

### 节点()

```rust
pub fn node(&self, id: NodeId) -> Option<&CognitiveNode>;
```

返回对具有给定 ID 的节点的引用，如果不存在则返回 `None` 。

---

### 邻居()

```rust
pub fn neighbors(
    &self,
    id: NodeId,
    direction: Direction,
) -> Vec<(NodeId, &CognitiveEdge)>;
```

返回指定方向上的所有相邻节点及其连接边。

**参数：**

|参数|类型 |描述 |
|-----------|------|-------------|
| `id` | `NodeId` |要查询其邻居的节点。 |
| `direction` | `Direction` | `Direction::Outgoing`、`Direction::Incoming` 或 `Direction::Both`。 |

---

### 遍历()

```rust
pub fn traverse(
    &self,
    start: NodeId,
    max_depth: usize,
    edge_filter: Option<&[EdgeType]>,
) -> TraversalResult;
```

从起始节点到指定深度执行广度优先遍历。

**参数：**

|参数|类型 |描述 |
|-----------|------|-------------|
| `start` | `NodeId` |遍历的起始节点。 |
| `max_depth` | `usize` |最大遍历深度。 |
| `edge_filter` | `Option<&[EdgeType]>` |如果提供，则仅遵循这些类型的边缘。 |

**返回：** `TraversalResult` 包含访问过的节点、遍历过的边以及到达的深度。

---

### 节点计数() / 边缘计数()

```rust
pub fn node_count(&self) -> usize;
pub fn edge_count(&self) -> usize;
```

返回图中节点和边的总数。

---

###nodes_by_type()

```rust
pub fn nodes_by_type(&self, event_type: EventType) -> Vec<NodeId>;
```

返回与给定事件类型匹配的所有节点 ID。

---

###nodes_by_session()

```rust
pub fn nodes_by_session(&self, session: u32) -> Vec<NodeId>;
```

返回属于给定会话的所有节点 ID。

---

## 类型

### 事件类型

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum EventType {
    Fact,
    Decision,
    Inference,
    Correction,
    Skill,
    Episode,
}
```

### 边缘类型

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum EdgeType {
    CausedBy,
    Supports,
    Contradicts,
    Supersedes,
    RelatedTo,
    PartOf,
    TemporalNext,
}
```

### 认知节点

```rust
pub struct CognitiveNode {
    pub id: NodeId,
    pub event_type: EventType,
    pub content: String,
    pub session: u32,
    pub confidence: f32,
    pub timestamp: i64,                          // Unix timestamp (seconds)
    pub metadata: HashMap<String, String>,
    pub vector: Option<Vec<f32>>,                // 128-dim feature vector
}
```

### 认知边缘

```rust
pub struct CognitiveEdge {
    pub source: NodeId,
    pub target: NodeId,
    pub edge_type: EdgeType,
    pub weight: f32,
}
```

### 遍历结果

```rust
pub struct TraversalResult {
    pub nodes: Vec<NodeId>,
    pub edges: Vec<(NodeId, NodeId, EdgeType)>,
    pub depth_reached: usize,
}
```

＃＃＃ 方向

```rust
pub enum Direction {
    Outgoing,
    Incoming,
    Both,
}
```

### 图错误

```rust
#[derive(Debug, thiserror::Error)]
pub enum GraphError {
    #[error("node {0} not found")]
    NodeNotFound(NodeId),
    #[error("invalid edge: {0}")]
    InvalidEdge(String),
    #[error("I/O error: {0}")]
    Io(#[from] std::io::Error),
    #[error("format error: {0}")]
    Format(String),
}
```

---

## 文件输入/输出

### 文件写入器

将 `CognitiveGraph` 序列化为 `.amem` 二进制格式。

```rust
use agentic_memory::io::FileWriter;

impl FileWriter {
    /// Writes the graph to the specified file path.
    /// Creates the file if it does not exist; overwrites if it does.
    pub fn write(graph: &CognitiveGraph, path: &Path) -> Result<(), GraphError>;
}
```

**例子：**

```rust
use agentic_memory::io::FileWriter;
use std::path::Path;

FileWriter::write(&graph, Path::new("agent.amem"))?;
```

### 文件阅读器

将 `.amem` 文件反序列化为 `CognitiveGraph`。将整个文件读入内存。

```rust
use agentic_memory::io::FileReader;

impl FileReader {
    /// Reads a .amem file and returns the full graph.
    pub fn read(path: &Path) -> Result<CognitiveGraph, GraphError>;
}
```

**例子：**

```rust
use agentic_memory::io::FileReader;
use std::path::Path;

let graph = FileReader::read(Path::new("agent.amem"))?;
println!("Loaded {} nodes", graph.node_count());
```

### MmapReader

内存映射读访问。 Does not load the entire file into memory -- pages are loaded on demand by the OS. Ideal for large brain files or read-heavy workloads.

```rust
use agentic_memory::io::MmapReader;

impl MmapReader {
    /// Opens a .amem file with memory-mapped I/O.
    pub fn open(path: &Path) -> Result<Self, GraphError>;

    /// Returns a reference to the node with the given ID.
    pub fn node(&self, id: NodeId) -> Option<CognitiveNodeRef<'_>>;

    /// Returns the total number of nodes.
    pub fn node_count(&self) -> usize;

    /// Returns the total number of edges.
    pub fn edge_count(&self) -> usize;

    /// Returns edges for a given node.
    pub fn edges(&self, id: NodeId, direction: Direction) -> Vec<CognitiveEdgeRef<'_>>;
}
```

**例子：**

```rust
use agentic_memory::io::MmapReader;
use std::path::Path;

let reader = MmapReader::open(Path::new("large_brain.amem"))?;
if let Some(node) = reader.node(NodeId(42)) {
    println!("Node 42: {}", node.content());
}
```

---

## 查询引擎

在 `CognitiveGraph` 或 `MmapReader` 之上提供索引查询操作。

```rust
use agentic_memory::query::QueryEngine;

impl QueryEngine {
    /// Creates a query engine from an in-memory graph.
    pub fn from_graph(graph: &CognitiveGraph) -> Self;

    /// Creates a query engine from a memory-mapped reader.
    pub fn from_mmap(reader: &MmapReader) -> Self;

    /// Returns all node IDs of the given event type.
    pub fn by_type(&self, event_type: EventType) -> Vec<NodeId>;

    /// Returns all node IDs in the given session.
    pub fn by_session(&self, session: u32) -> Vec<NodeId>;

    /// Performs a BFS traversal from the starting node.
    pub fn traverse(
        &self,
        start: NodeId,
        max_depth: usize,
        edge_filter: Option<&[EdgeType]>,
    ) -> TraversalResult;

    /// Finds the top-k most similar nodes to the given query vector.
    pub fn similarity_search(
        &self,
        query_vector: &[f32; 128],
        top_k: usize,
    ) -> Vec<(NodeId, f32)>;
}
```

**例子：**

```rust
use agentic_memory::query::QueryEngine;

let engine = QueryEngine::from_graph(&graph);

// Find all facts
let facts = engine.by_type(EventType::Fact);

// Similarity search
let results = engine.similarity_search(&query_vec, 10);
for (node_id, score) in results {
    println!("Node {}: score {:.3}", node_id.0, score);
}
```

---

## v0.2 查询方法

v0.2.0 中添加。所有方法均可在 `QueryEngine` 上使用。

### 搜索文本()

```rust
pub fn search_text(&self, query: &str, top_k: usize) -> Vec<TextMatch>;
```

使用倒排术语索引的 BM25 文本搜索。 Falls back to full scan for v0.1 files without a TermIndex.

**性能：** 1.58 ms @ 100K 节点（快速路径），122 ms（慢速路径）。

---

### search_hybrid()

```rust
pub fn search_hybrid(
    &self,
    query: &str,
    query_vector: &[f32; 128],
    top_k: usize,
    alpha: f32,
) -> Vec<HybridMatch>;
```

将 BM25 + 余弦相似度搜索与 RRF（倒数排名融合）相结合。 `alpha` 控制 BM25/向量权重平衡（0.0 = 纯向量，1.0 = 纯 BM25）。

**Performance:** 10.83 ms @ 100K nodes.

---

### 中心性()

```rust
pub fn centrality(&self, metric: CentralityMetric) -> Vec<(NodeId, f64)>;
```

计算所有节点的中心性分数。可用指标：`PageRank`、`Degree`、`Betweenness`。

**性能：** PageRank 34.3 毫秒，度数 20.7 毫秒，介数 10.1 秒 @ 100K 节点。

---

###最短路径()

```rust
pub fn shortest_path(
    &self,
    src: NodeId,
    dst: NodeId,
    algorithm: PathAlgorithm,
) -> Option<PathResult>;
```

查找两个节点之间的最短路径。 Available algorithms: `BFS` (unweighted), `Dijkstra` (weighted).

**性能：** BFS 104 µs，Dijkstra 17.6 ms @ 100K 节点。

---

＃＃＃ 修订（）

```rust
pub fn revise(&self, node_id: NodeId) -> RevisionReport;
```

反事实信念修正：计算如果给定节点被撤回将会发生什么变化的级联。只读 - 不修改图表。

**性能：** 100K 节点时为 53.4 毫秒。

---

### 间隙()

```rust
pub fn gaps(&self) -> GapReport;
```

识别推理弱点：无支持的决策、孤立的事实、未经纠正的矛盾以及薄弱的推理链。

**Performance:** 297 s @ 100K nodes (offline tier). Completes in \<3 s at 10K nodes.

---

### 类比()

```rust
pub fn analogy(&self, node_id: NodeId, top_k: usize) -> Vec<Analogy>;
```

结构模式匹配：查找与给定节点的局部邻域相​​似的子图模式。

**Performance:** 229 s @ 100K nodes (offline tier). Completes in \<3 s at 10K nodes.

---

### 合并()

```rust
pub fn consolidate(&self, dry_run: bool) -> ConsolidationReport;
```

图维护：检测重复内容、链接矛盾并建议合并。当 `dry_run` 为 true 时，返回报告而不修改图形。

**性能：** 43.6 s @ 100K 节点（周期层）。

---

### 漂移()

```rust
pub fn drift(&self) -> DriftReport;
```

分析取代链以追踪信念如何随时间演变。

**性能：** 100K 节点时为 68.4 毫秒。

---

## CLI Reference: `amem`

`amem` 二进制文件提供对所有核心操作的命令行访问。

### amem create

创建一个新的空大脑文件。

```
amem create <PATH>
```

|论证|描述 |
|----------|-------------|
| `PATH` |新 `.amem` 文件的路径。 |

**例子：**

```bash
amem create project.amem
```

---

### 添加

将认知事件添加到大脑文件中。

```
amem add <PATH> <TYPE> <CONTENT> [OPTIONS]
```

|参数/标志|描述 |
|-----------------|-------------|
| `PATH` | `.amem` 文件的路径。 |
| `TYPE` |事件类型：`fact`、`decision`、`inference`、`correction`、`skill`、`episode`。 |
| `CONTENT` |事件的文本内容（带引号的字符串）。 |
| `--confidence <FLOAT>` | Confidence score, 0.0 to 1.0. Default: 1.0. |
| `--session <INT>` | Session ID. Default: auto-assigned. |
| `--meta <KEY=VALUE>` |元数据键值对。可重复。 |

**例子：**

```bash
amem add project.amem fact "Rust 1.75 supports async traits" --confidence 0.95 --meta source=docs
```

---

### 链接

在两个事件之间创建边缘。

```
amem link <PATH> <SOURCE> <TARGET> <TYPE> [OPTIONS]
```

|参数/标志|描述 |
|-----------------|-------------|
| `PATH` | `.amem` 文件的路径。 |
| `SOURCE` |源节点 ID（整数）。 |
| `TARGET` |目标节点 ID（整数）。 |
| `TYPE` |边缘类型：`caused_by`、`supports`、`contradicts`、`supersedes`、`related_to`、`part_of`、`temporal_next`。 |
| `--weight <FLOAT>` | Edge weight, 0.0 to 1.0. Default: 1.0. |

**例子：**

```bash
amem link project.amem 0 1 supports --weight 0.9
```

---

### 阿姆信息

显示有关大脑文件的摘要信息。

```
amem info <PATH> [OPTIONS]
```

|参数/标志|描述 |
|-----------------|-------------|
| `PATH` | `.amem` 文件的路径。 |
| `--sessions` |显示每个会话的细分。 |
| `--json` |输出为 JSON。 |

**例子：**

```bash
amem info project.amem --sessions
```

---

### ame遍历

从起始节点遍历图形并打印结果。

```
amem traverse <PATH> <START_ID> [OPTIONS]
```

|参数/标志|描述 |
|-----------------|-------------|
| `PATH` | `.amem` 文件的路径。 |
| `START_ID` |起始节点ID。 |
| `--depth <INT>` | Maximum traversal depth. Default: 3. |
| `--edge-type <TYPE>` | Filter to specific edge types. Repeatable. |
| `--json` |输出为 JSON。 |

**例子：**

```bash
amem traverse project.amem 0 --depth 5 --edge-type supports --edge-type caused_by
```

---

### 查询

按类型、会话或相似性搜索查询大脑中的事件。

```
amem query <PATH> [OPTIONS]
```

|旗帜|描述 |
|------|-------------|
| `--event-types <TYPE>` | Filter by event type. |
| `--session <INT>` | Filter by session ID. |
| `--search <TEXT>` | Semantic similarity search query. |
| `--top-k <INT>` | Number of results for similarity search. Default: 10. |
| `--min-confidence <FLOAT>` | Minimum confidence threshold. |
| `--json` |输出为 JSON。 |

**例子：**

```bash
amem query project.amem --event-types fact --min-confidence 0.8
amem query project.amem --search "database performance" --top-k 5
```

---

### amem mcp-serve

启动 MCP（模型上下文协议）服务器，将大脑暴露为 AI 编码助手的工具。

```
amem mcp-serve <PATH> [OPTIONS]
```

|参数/标志|描述 |
|-----------------|-------------|
| `PATH` | `.amem` 文件的路径。 |
| `--port <INT>` | Port to listen on. Default: 3100. |
| `--host <ADDR>` | Host address to bind. Default: `127.0.0.1`. |
| `--read-only` |以只读模式提供服务（不允许写入）。 |

**例子：**

```bash
amem mcp-serve project.amem --port 3100
```

这将大脑暴露为 Claude Code、Cursor、Windsurf 和其他 MCP 兼容编辑器可以连接的 MCP 工具。有关配置详细信息，请参阅[集成指南](/docs/zh/memory-integration-guide)。

---

### v0.2 CLI 命令

这些命令是在 v0.2.0 中添加的。

#### amem 文本搜索

BM25 文本搜索。

```bash
amem text-search <PATH> <QUERY> [--top-k <INT>]
```

#### amem 混合搜索

混合 BM25 + 带 RRF 融合的矢量搜索。

```bash
amem hybrid-search <PATH> <QUERY> [--top-k <INT>] [--alpha <FLOAT>]
```

#### aem中心性

计算中心性分数。

```bash
amem centrality <PATH> --metric <pagerank|degree|betweenness> [--top-k <INT>] [--json]
```

#### 路径

找到两个节点之间的最短路径。

```bash
amem path <PATH> <SRC_ID> <DST_ID> [--algorithm <bfs|dijkstra>] [--json]
```

#### 修改

运行反事实信念修正级联。

```bash
amem revise <PATH> <NODE_ID> [--json]
```

#### 差距

检测推理差距和弱点。

```bash
amem gaps <PATH> [--json]
```

####阿姆类比

查找节点的结构类比。

```bash
amem analogy <PATH> <NODE_ID> [--top-k <INT>] [--json]
```

#### aem 巩固

运行图合并（重复数据删除、矛盾链接）。

```bash
amem consolidate <PATH> [--dry-run] [--json]
```

####阿姆漂移

检测取代链中的信念漂移。

```bash
amem drift <PATH> [--json]
```
